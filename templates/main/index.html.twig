{% extends 'base.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block main %}
    <h1>{{ message }}</h1>
    {{ dump(data) }}
    {% for key,value in data %}
        <p><a href="{{ key }}">{{ value }}</a></p>
    {% endfor %}
{% endblock %}
